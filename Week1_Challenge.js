
let area;

function calcArea (base, height){
    return area = (base * height)/2
};
let height = 6
let base = 10
console.log("The area of a rectangle with a height of " + height + 
" and base of " + base+ " is " + calcArea(base, height) + ".")