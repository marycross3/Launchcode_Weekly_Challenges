// Create a method in the Person class which returns how another person's age compares. Given the 
// instances p1, p2 and p3, which will be initialised with the attributes name and age, return a 
// sentence in the following format: {other person name} is {older than / younger than / the same age as} me

//https://github.com/marycross3/Launchcode_Weekly_Challenges.git